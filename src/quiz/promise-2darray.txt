Explain the code execution based on the order of the messages logged in src/quiz/promise-2darray.txt.

Both the sumPromise1 and sumPromise2 are micro task in the even loop. They are both put in to the micro task callback queue at the same time. During the execution, the "Sum called ... " is the synchronize operation, and the setTimeOut is macro task, which executed at the last. 